#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

//int isPrime(int n) { //소수(prime) 여부 판별 함수
//	for (int i = 2; i*i <= n; i++)
//		if (!(n % i)) // if(n%i == 0)
//			return 0;
//	return 1;
//}

int main_prime() {
	/*printf("숫자 : ");
	int a = 1, cnt = 0;
	scanf("%d", &a);
	if (a <= 0 || a > 1000)
		break;
	for (int i = 2; i < a; i++) {
		if (isPrime(i)) {
			printf("%4d ", i);
			if (cnt % 10 == 0)
				printf("\n");
			cnt++;
		}
	}*/
	return 0;
}