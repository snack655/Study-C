#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
//십진수를 16진수와 2진수로 바꿔 출력하시오.
int main_05_18_atoi_itoa() {
	//int i;
	//char arr[50];
	//char str[256]; // 1차원 배열
	//printf("Enter a number: ");
	//scanf("%d", &i);
	//_itoa(i, arr, 10);
	//printf("decimal : %s\n", arr);
	//_itoa(i, arr, 16);
	//printf("hexadecimal : %s\n", arr);
	//_itoa(i, arr, 2);
	//printf("binary : %s\n", arr);
	// char* itoa(int value, char* str, int radix);

	//fgets(buffer, 256, stdin);
	//gets(str, 256); 
	//i = atoi(str); // 문자열을 숫자로 변환
	////atoi = char to int = 문자열을 정수 타입으로
	////atof = char to double = 문자열을 실수 타입으로
	////atol = char to long int = 문자열을 long 정수 타입으로
	//printf("입력된 정수값 : %d, 2배의 값: %d. \n", i, i * 2);
	return 0;
}